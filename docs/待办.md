# 待办

记录排期中的功能 / 改动，做完一项划掉一项。

---

## 明天（2026-06-12）

- [ ] **电台详情页 UI**：把电台详情页的界面调出来。电台功能代码已基本就绪，但入口当前锁为"开发中"（phone.js，与邮箱同款拦截），需要把详情页 UI 完成后再解锁入口。
- [ ] **寄快递 / 查快递功能**：新增寄快递、查快递能力。
- [ ] **日历提示功能**：改一下日历的提醒/提示逻辑（具体改法待 0 号明确）。
- [ ] **番茄钟功能**：新增番茄钟。

---

## 备注

- 电台入口解锁点：`js/phone.js` 中 `if (appId === 'radio') { UI.showToast('电台开发中...', 1500); return; }`，详情页做好后删掉这行即可放行。
- 电台设计文档见 `docs/电台设计.md`。
